이 코드는 **"넣는 작업과 빼는 작업이 동시에 일어나지 않는다"**는 현재의 구조에 완벽하게 결합되어 있습니다.

나중에 시스템을 실시간(Real-time) 레이더에 연결해서
**"0번 코어가 실시간으로 데이터를 수신해서 큐에 계속 Push하고,
워커들이 동시에 Pop하는 구조"**로 바꾸게 된다면, 
이 코드는 그 즉시 터집니다. 
그때는 이전에 알려드렸던 atomic 기반의 락-프리 큐로 교체하셔야 합니다.